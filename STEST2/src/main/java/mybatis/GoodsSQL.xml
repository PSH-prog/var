<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="goods">

 <select id="selectGoodsList" resultType="goodsvo">
	<![CDATA[
		SELECT * FROM Goods
	]]>
 </select>

 <insert id="insertGoods" parameterType="hashMap" useGeneratedKeys="true" keyProperty="goods_id">
 	<![CDATA[
 		INSERT INTO Goods (gcode, gname,gprice,gtotal) VALUES (#{gcode, jdbcType=VARCHAR}, #{gname, jdbcType=VARCHAR}, #{gprice, jdbcType=VARCHAR}, #{gtotal,jdbcType=VARCHAR})
 	]]>
 </insert>
    
 <delete id="deleteGoods" parameterType="hashMap" >
 	<![CDATA[
		DELETE FROM Goods WHERE gcode = #{gcode}
	]]>
 </delete> 

</mapper>